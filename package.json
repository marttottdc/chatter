{
  "scripts": {
    "build": "tailwindcss build -c tailwind.config.js -o static/site.css --minify",
    "watch": "tailwindcss build -c tailwind.config.js -o static/site.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}