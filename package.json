{
  "scripts": {
    "build": "tailwindcss build -i frontend/input.css -c tailwind.config.js -o static/site.css --minify",
    "watch": "tailwindcss build -i frontend/input.css -c tailwind.config.js -o static/site.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}